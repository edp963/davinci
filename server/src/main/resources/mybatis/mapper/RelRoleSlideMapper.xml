<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edp.davinci.dao.RelRoleSlideMapper">
    <insert id="insert" parameterType="edp.davinci.model.RelRoleSlide">
        <selectKey resultType="java.lang.Long" order="AFTER" keyProperty="id">
            SELECT id from rel_role_slide where role_id = #{roleId} and slide_id = #{slideId}
        </selectKey>
        insert ignore rel_role_slide
        <trim prefix="(" suffix=")" suffixOverrides=",">
            role_id,
            slide_id,
            permission,
            create_by,
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            #{roleId,jdbcType=BIGINT},
            #{slideId,jdbcType=BIGINT},
            #{permission,jdbcType=SMALLINT},
            #{createBy,jdbcType=BIGINT},
            #{createTime,jdbcType=TIMESTAMP}
        </trim>
    </insert>
</mapper>